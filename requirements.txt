# Optional compatibility file. pyproject.toml is the preferred source of truth.
# ── Core ML dependencies ───────────────────────────────────────────────────────
numpy>=1.24.0
scipy>=1.9.0
nilearn>=0.10.0
nibabel>=4.0.2
scikit-image>=0.20.0

# Visualization / development
tqdm>=4.64.0
matplotlib>=3.7.0
seaborn>=0.12.0

# LLM inference — transformers backend (Windows-compatible)
#transformers>=4.40.0
#accelerate>=0.30.0
# vllm>=0.4.0   


